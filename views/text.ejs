<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
      <p><%= scenario.text %></p>
      <br/>
      <p><strong>Question</strong>: <%= scenario.question %></p>
      <br/>
	  <p> Only <%= answerer %> needs to type out the answer for the entire group %> </p>
	  <p> All other team members should wait until the answer is submitted before proceeding </p>
      <form action="/response" method="post">
      	<% if (role == answerer) { %>
	        <input type="text" name="answer">
	    <% } %>
	    <input type="hidden" value="<%= role %>" name="role"/>
	    <input type="hidden" value="<%= description %>" name="description"/>
	    <input type="submit" value="Proceed" />
      </form>
  </body>
</html>

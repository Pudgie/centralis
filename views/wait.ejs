<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <p>Your role is <%= role %>.</p>
    <p><%= description %></p>
    <input type="hidden" value="<%= role %>" name="role"/>
    <input type="hidden" value="<%= description %>" name="description"/>
    <button type="submit" onclick="location.href = '/startScenario'">Start</button>
  </body>
</html>
